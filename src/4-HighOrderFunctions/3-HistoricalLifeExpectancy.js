/*
Average of people in the ancestry file. Group the results
by century.
 */

export const fromCentury = date => (
  Math.ceil(date / 100)
);

const historicalLifeExpectancy = () => (
  true
);

export default historicalLifeExpectancy;

